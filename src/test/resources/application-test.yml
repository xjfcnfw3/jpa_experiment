spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
logging:
  level:
    org:
      hibernate:
        SQL: info
        type:
          descriptor:
            sql:
              BasicBinder: warn
    root: warn
